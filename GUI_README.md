# AlphaSnobAI Desktop GUI

Кроссплатформенное десктопное приложение для управления AlphaSnobAI ботом.

## Установка зависимостей

```bash
# Установите GUI зависимости
pip install -r requirements-gui.txt
```

## Запуск

```bash
# Запуск GUI приложения
python3 gui_launcher.py

# Или напрямую
python3 -m gui.app
```

## Функции

### Реализовано ✅

1. **Dashboard (Главная панель)**
   - Управление ботом (Start/Stop/Restart)
   - Статус бота в реальном времени
   - PID, Uptime, CPU, Memory
   - Индикатор "Требуется перезапуск"

2. **Logs (Логи)**
   - Просмотр логов в реальном времени
   - Цветовая индикация уровней (DEBUG, INFO, SUCCESS, WARNING, ERROR)
   - Фильтрация по уровню
   - Поиск по тексту
   - Автопрокрутка (вкл/выкл)
   - Экспорт логов в файл
   - Очистка экрана

3. **Главное окно**
   - Боковое меню навигации
   - Строка состояния с информацией о боте
   - Темная тема

### В разработке 🚧

4. **Settings (Настройки)**
   - Редактирование всех настроек бота
   - Валидация параметров
   - Backup конфига перед изменениями

5. **Owner Learning (Обучение владельца)**
   - Настройка owner_user_ids
   - Сбор сообщений из базы
   - Анализ стиля
   - Просмотр образцов

6. **Statistics (Статистика)**
   - Общая статистика
   - По чатам
   - По пользователям
   - По персонам

7. **Database (База данных)**
   - Backup/Restore
   - Очистка старых сообщений
   - Оптимизация (VACUUM)
   - Экспорт чатов

## Горячие клавиши

- `Ctrl+Q` - Выход
- `Ctrl+S` - Запустить бота
- `Ctrl+T` - Остановить бота
- `Ctrl+R` - Перезапустить бота

## Архитектура

```
gui/
├── app.py                 # Главное приложение (QtAsyncio)
├── main_window.py         # Главное окно с навигацией
├── backend/               # Бэкенд сервисы
│   └── bot_process.py     # Управление процессом бота (QProcess)
├── widgets/               # UI виджеты
│   ├── bot_control.py     # Панель управления ботом
│   └── log_viewer.py      # Просмотр логов
└── resources/             # Ресурсы (иконки, стили)
```

## Технологии

- **PySide6** (Qt6) - GUI фреймворк
- **QtAsyncio** - Интеграция asyncio с Qt
- **qdarkstyle** - Темная тема
- **psutil** - Мониторинг процесса

## Упаковка приложения

### macOS

```bash
# Установить PyInstaller
pip install pyinstaller

# Собрать .app
pyinstaller --windowed --name AlphaSnobAI gui_launcher.py
```

### Windows

```bash
pip install pyinstaller
pyinstaller --windowed --onefile --name AlphaSnobAI gui_launcher.py
```

### Linux

```bash
pip install pyinstaller
pyinstaller --windowed --name AlphaSnobAI gui_launcher.py
```

## Разработка

Для добавления нового экрана:

1. Создайте виджет в `gui/widgets/your_widget.py`
2. Импортируйте его в `gui/main_window.py`
3. Добавьте в `_add_screens()` метод
4. Добавьте пункт в боковое меню

## Решение проблем

### PySide6 не устанавливается

```bash
# Попробуйте установить через conda
conda install pyside6

# Или обновите pip
pip install --upgrade pip
pip install PySide6
```

### Бот не запускается из GUI

- Проверьте, что `main.py` работает из командной строки
- Проверьте логи в разделе "Logs"
- Убедитесь, что config.yaml корректен

### Темная тема не применяется

- Установите qdarkstyle: `pip install qdarkstyle`
- При отсутствии qdarkstyle используется базовая темная палитра

## Следующие шаги

1. Доработать Settings Editor с формами для всех настроек
2. Реализовать Owner Learning UI
3. Добавить Statistics с графиками
4. Создать Database Manager
5. Добавить системный трей
6. Упаковать в .app/.exe для распространения
